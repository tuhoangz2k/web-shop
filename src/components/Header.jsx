import React from 'react';
import PropTypes from 'prop-types';

Header.propTypes = {};

function Header(props) {
    return (
        <div className="grid">
            <div className="grid wide">Hellow</div>
        </div>
    );
}

export default Header;
